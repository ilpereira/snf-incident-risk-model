[pytest]
testpaths = tests
# Coverage is NOT enabled by default because the C tracer conflicts with
# SHAP/XGBoost C extensions in WSL2 and causes intermittent segfaults.
# Run coverage explicitly:
#   python -m pytest --cov=scripts --cov-report=term-missing --cov-config=.coveragerc
